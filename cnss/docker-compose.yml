services:
  cnss:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ENV=development
      - PORT=8000
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload