[pytest]
# Automatically detect async tests without needing @pytest.mark.asyncio everywhere
asyncio_mode = auto
# Explicitly set the loop scope to prevent future deprecation warnings
asyncio_default_fixture_loop_scope = function